version: "3"

services:
  server:
    image: whatsapp-not-a-contact-bot
    build: .
    restart: always
    volumes:
      - ${PWD}/logs:/app/logs
    environment:
      LOGS_FOLDER: /app/logs
